{
  "_comment": "HyperPod ノードの役割を決める設定。lifecycle_script.py は自ノードのインスタンスグループ名を見て、controller_group と一致すれば HEAD_NODE(Slurm controller)、一致しなければ COMPUTE_NODE と判定する(排他的)。そのため controller と worker は別インスタンスグループに分ける必要がある。",
  "workload_manager": "slurm",
  "controller_group": "controller"
}
